name: Build lxrad
on: [push]
jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install deps
        run: sudo apt-get install automake autoconf
      - name: Build
        run: |
          ./make_deps.sh
          autoconf
          ./configure
          make

